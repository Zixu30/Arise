ARISEE FINAL GITHUB PACKAGE

Upload EVERYTHING inside this folder to the GitHub repository root:
index.html
css/styles.css
js/app.js
assets/payment-qr.jpg

GitHub Pages: Settings > Pages > Deploy from branch > main > /(root).

The index contains a safe CSS fallback so the UI remains styled even if css/styles.css is accidentally not uploaded. The main checkout flow is self-contained in index.html.
